/**
 * @constant `spacing` in space units from theme.
 * @constant `minChildWidth` in pixels.
 */
export type GridContext = {
  spacing?: number;
  minChildWidth?: number;
  gridTemplateColumns?: string;
};
